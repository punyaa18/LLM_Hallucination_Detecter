% TikZ figure: Methodology Feature Module Diagram
% Usage in paper preamble:
%   \usepackage{tikz}
%   \usetikzlibrary{arrows.meta,positioning}
%
% Usage in paper body:
%   \input{figures/methodology_feature_modules_tikz.tex}

\begin{figure*}[!t]
\centering
\begin{tikzpicture}[
    x=1cm,
    y=1cm,
    >=Stealth,
    module/.style={
      draw=blue!55!black,
      line width=0.8pt,
      rectangle,
      minimum width=3.55cm,
      minimum height=2.0cm,
      align=center,
      fill=blue!3,
      font=\small
    },
    flow/.style={->, line width=0.8pt, draw=blue!60!black}
  ]

  % Main pipeline modules
  \node[module] (input) at (0,0) {\textbf{Input Layer}\\[2pt]Query / text};
  \node[module] (claim) at (4.0,0) {\textbf{Claim Extraction}\\[2pt]Atomic claims};
  \node[module] (evidence) at (8.0,0) {\textbf{Evidence Retrieval}\\[2pt]Trusted sources};
  \node[module] (verify) at (12.0,0) {\textbf{Verification}\\[2pt]NLI / similarity};
  \node[module] (detect) at (16.0,0) {\textbf{Hallucination Detection}\\[2pt]Supp. / Contr. / Unverif.};
  \node[module] (risk) at (20.0,0) {\textbf{Risk Scoring}\\[2pt]Confidence score};
  \node[module] (output) at (24.0,0) {\textbf{Annotated Response}\\[2pt]Flags + reliability};

  % Data flow arrows
  \draw[flow] (input.east) -- (claim.west);
  \draw[flow] (claim.east) -- (evidence.west);
  \draw[flow] (evidence.east) -- (verify.west);
  \draw[flow] (verify.east) -- (detect.west);
  \draw[flow] (detect.east) -- (risk.west);
  \draw[flow] (risk.east) -- (output.west);

  % Optional stage labels
  \node[font=\small\bfseries, text=blue!70!black] at (0,1.55) {Input};
  \node[font=\small\bfseries, text=blue!70!black] at (4,1.55) {Extraction};
  \node[font=\small\bfseries, text=blue!70!black] at (12,1.55) {Verification};
  \node[font=\small\bfseries, text=blue!70!black] at (20,1.55) {Scoring \& Output};

\end{tikzpicture}
\caption{Methodology feature module diagram for hallucination detection in LLM outputs.}
\label{fig:methodology_tikz}
\end{figure*}
